#include <reg52.h>
void main(void)
{
	//定义一些随机数据，数据存放在片内CODE 区中
	unsigned char code date[]={
	0xFF,0xFE,0xFD,0xFB,0xF7,0xEF,0xDF,0xBF,
	0x7F,0x7F,0xBF,0xDF,0xEF,0xF7,0xFB,0xFD,
	0xFE,0xFF,0xFF,0xFE,0xFC,0xF8,0xF0,0xE0,
	0xC0,0x80,0x0,0xE7,0xDB,0xBD,0x7E,0xFF};
	unsigned int a; //定义循环用的变量
	unsigned char b;
	unsigned char code * finger; //定义基于CODE 区的指针
	do
	{
		finger = &date[0]; //取得数组第一个单元的地址
		for (b=0;b<32;b++)
		{
			for(a=0; a<30000; a++); //延时一段时间
			P1 = *finger; //从指针指向的地址取数据到 P1 口
			finger++; //指针加一，
		}
	}
	while(1);
}


